[Unit]
Description=Daily Thrushcombe tick (companion mode: one sim-day per real-day)

[Timer]
# Just after midnight; Persistent fires it on next boot if the machine was off.
OnCalendar=*-*-* 00:10:00
Persistent=true

[Install]
WantedBy=timers.target
