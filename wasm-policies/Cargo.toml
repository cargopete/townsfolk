[package]
name = "wasm-policies"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
policies = { path = "../policies" }

[profile.release]
panic = "abort"
opt-level = "z"
strip = true
