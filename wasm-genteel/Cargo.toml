[package]
name = "wasm-genteel"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
policy-genteel = { path = "../policy-genteel" }

[profile.release]
panic = "abort"
opt-level = "z"
strip = true
